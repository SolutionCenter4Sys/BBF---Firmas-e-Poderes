# Front (WF-13). Copiar para .env.local.
# Sem chave KAAS — KAAS só no Worker .NET (Kas__ApiKey / Kas__RunUrl).

NEXT_PUBLIC_API_URL=http://localhost:8080

# Selo "MVP mock · v0.1" na sidebar. Só development/staging.
# Sem esta variável (demo/produto) o selo não aparece.
# NEXT_PUBLIC_APP_ENV=development
